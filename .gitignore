node_modules/
dist/
*.log
.env
.env.local
.DS_Store

# Local Claude config — but keep the shipped skill (.claude/skills/) tracked,
# since the docs reference it and contributors install it.
.claude/*
!.claude/skills/
